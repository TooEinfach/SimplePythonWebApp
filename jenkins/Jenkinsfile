pipeline {
    agent none
    stages {
        stage('Build') {
           script {
               image = docker.build("${IMAGE}")
               println "Newly generated image, " +image.id
           }
        }
    }
}