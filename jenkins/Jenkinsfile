pipeline {
    agent none
    stages {
        stage('Build') {
           steps {
            script {
                image = docker.build("/home/pi/gitDirectory/SimplePythonWebApp")
                println "Newly generated image, " +image.id
            }
           }
        }

    }
}